export default function Video({texto,paragrafo}) {
    return(
        <section>
            <p>{paragrafo}</p>

            <h4>{texto}</h4>
            <div>
                <p>play</p>
            </div>
        </section>
    )
}